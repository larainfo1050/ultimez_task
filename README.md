Display Serial Number, First Name, Last Name, City, Country, Mobile Number and Date & Time in Table
Take one Search input textbox and it should contain, search button and reset button.
The Search input textbox should filter the First Name, Last Name, City and Country in the table.
The Reset button is also used for reset the pagination in table.
Add pagination with limit 10 for each page. The pagination should work with search filter.
When Searched in the input textbox, If there is no data then show a messag read this 